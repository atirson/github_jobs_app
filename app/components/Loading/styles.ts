import styled from 'styled-components/native';
import {theme} from '@cuteapp/constants/theme';

export const LoadingIcon = styled.ActivityIndicator`
  color: ${theme.colors.opposity};
  margin: 20px 0;
`;
